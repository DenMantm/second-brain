# ===========================================
# Second Brain - Docker Environment Variables
# ===========================================

# ===== SERVER CONFIGURATION =====
NODE_ENV=production
PORT=3030
HOST=0.0.0.0
LOG_LEVEL=info

# ===== CORS CONFIGURATION =====
# Allow client to connect from Docker network
CORS_ORIGIN=http://localhost:8080

# ===== SERVICE URLs (Docker Network) =====
# These use Docker service names for inter-container communication
TTS_SERVICE_URL=http://tts-service:3002
STT_SERVICE_URL=http://stt-service:3003

# LLM Service (running on host machine via LM Studio or llama.cpp)
# Use host.docker.internal to access host machine from container
LLM_SERVICE_URL=http://host.docker.internal:1234/v1

# ===== OPTIONAL: WAKE WORD =====
# Porcupine access key (if using wake word detection)
PORCUPINE_ACCESS_KEY=

# ===========================================
# NOTES:
# - Do NOT commit this file with real credentials
# - Copy to .env.production for production use
# - Adjust URLs if deploying to different hosts
# ===========================================
